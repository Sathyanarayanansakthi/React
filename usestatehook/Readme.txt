use State hook is  a react hook

 before use State hook you need to learn what is State

 State is any point of information that can change over time across renders in react
 
 example :insta likes and follow (at is not fixed right its always changes right )

 UseState hook is used to mainpulate the states in react 
 and we are going to learn by using counter application in react js + vite 


 Steps to useState hook
 
 Step 1: Create a project in your local machine using vite or react (commands give below)
 Step 2: give commands run the application as npm run dev
 Step 3: use syntax give below
 const [stateVariable, setStateFunction] = useState(initialValue);
